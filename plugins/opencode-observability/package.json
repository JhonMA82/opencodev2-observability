{
  "name": "@opencode-observability/plugin",
  "version": "2.0.0",
  "description": "OpenCode V2 plugin for observability and event tracking (OpenCode >= 2.0.4 < 3 only)",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode/plugin": "2.0.12"
  },
  "devDependencies": {
    "@types/bun": "1.2.2",
    "typescript": "5.3.3"
  },
  "engines": {
    "opencode": ">=2.0.4 <3"
  }
}
